# Exclude the entire backend from EAS builds (only the mobile app is needed)
../backend/

# Exclude local H2 database files
*.mv.db
*.trace.db

# Exclude large files not needed for the build
../backend/data/
